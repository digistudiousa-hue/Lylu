// Single source of truth for the outbound app entry points. Every sign-up /
// sign-in CTA on the landing site resolves through these two constants, so the
// canonical app domain lives in exactly one place.

export const SIGNUP_URL = "https://app.lylu.ai/sign-up";
export const SIGNIN_URL = "https://app.lylu.ai/sign-in";
